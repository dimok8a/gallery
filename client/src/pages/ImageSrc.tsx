import React, { FC } from "react";
import { ImageModal } from "../components/ImageModal/ImageModal";

export const ImageSrc: FC = () => {
    return (
        <div>
            <ImageModal />
        </div>
    );
};
